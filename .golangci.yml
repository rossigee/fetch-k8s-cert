version: "2"

run:
  timeout: 5m
  issues-exit-code: 1
  tests: true

linters:
  enable:
    - bodyclose
    - dogsled
    - dupl
    - errcheck
    - gochecknoinits
    - goconst
    - govet
    - misspell
    - nakedret
    - rowserrcheck
    - staticcheck
    - unconvert
    - unused
    - whitespace
  disable:
    - depguard
    - gosec
    - gocyclo
    - gocritic
    - prealloc